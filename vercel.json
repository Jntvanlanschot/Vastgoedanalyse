{
  "buildCommand": "npm install && cd apps/workflow-py && pip3 install -r requirements.txt && cd ../.. && npm run build",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 300
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}


